/*
export function someMutation (state) {
}
*/

export function setID(state, id) {
  state.id = id;
}

export function setGrade(state, grade) {
  state.grade = grade;
}

export function setDataLoaded(state, dataLoaded) {
  state.dataLoaded = dataLoaded;
}
